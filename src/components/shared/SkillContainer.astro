---
interface Props {
  className?: string;
}

const { className } = Astro.props as Props;
---

<article
  class={`${className} max-w-full w-auto flex items-center gap-3`}
>
  <slot />
</article>
